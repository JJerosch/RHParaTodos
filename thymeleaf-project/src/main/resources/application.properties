# ===========================================
# CONFIGURAÇÕES DO SPRING BOOT
# Sistema de Gestão de RH
# ===========================================

# ===== SERVER =====
server.port=8080
server.servlet.context-path=/
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# ===== THYMELEAF =====
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html

# ===== STATIC RESOURCES =====
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=0
spring.web.resources.chain.strategy.content.enabled=true
spring.web.resources.chain.strategy.content.paths=/**

# ===== DATABASE (H2 para desenvolvimento) =====
spring.datasource.url=jdbc:h2:mem:rhdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===== DATABASE (PostgreSQL para produção) =====
# spring.datasource.url=jdbc:postgresql://localhost:5432/rhdb
# spring.datasource.username=postgres
# spring.datasource.password=sua_senha
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===== DATABASE (MySQL para produção) =====
# spring.datasource.url=jdbc:mysql://localhost:3306/rhdb?useSSL=false&serverTimezone=America/Sao_Paulo
# spring.datasource.username=root
# spring.datasource.password=sua_senha
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ===== SPRING SECURITY =====
# spring.security.user.name=admin
# spring.security.user.password=admin123

# ===== FILE UPLOAD =====
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Diretório para upload de arquivos
app.upload.dir=./uploads
app.upload.photos-dir=./uploads/fotos

# ===== LOGGING =====
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate=WARN
logging.level.com.sistema.rh=DEBUG

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ===== MESSAGES =====
spring.messages.basename=messages
spring.messages.encoding=UTF-8

# ===== DATE FORMAT =====
spring.jackson.date-format=dd/MM/yyyy
spring.jackson.time-zone=America/Sao_Paulo

# ===== ACTUATOR (opcional) =====
# management.endpoints.web.exposure.include=health,info
# management.endpoint.health.show-details=always

# ===== CUSTOM PROPERTIES =====
app.name=Sistema RH
app.version=1.0.0
app.company.name=Empresa LTDA

# Configurações de paginação
app.pagination.default-size=10
app.pagination.max-size=100

# Configurações de sessão
server.servlet.session.timeout=30m
